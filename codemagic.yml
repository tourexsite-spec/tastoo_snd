workflows:
  android-workflow:
    name: Tastoo SND Build
    max_build_duration: 60

    environment:
      flutter: stable
      java: 17

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
